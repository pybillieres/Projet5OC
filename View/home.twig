{% extends 'layout.twig' %}

{% block head %}<title>Accueil</title>{% endblock %}

{% block content %}
    <h1>Derniers Films</h1>
    {% for movie in movies %}
        <div>
        <img src="" class=movies id={{movie.idDb}}>
            <a href=Movie/MovieDetails/{{movie.id}}><h2>{{movie.title}}</h2></a>
            <p >{{movie.id}}</p>
            <p>{{movie.idDb}}</p>
        </div>
    {% endfor %}
  <script src="public/js/homeMain.js"></script>
{% endblock %}